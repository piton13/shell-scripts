#!/bin/bash

sudo apt install awscli

# to update awscli
python3 --version
pip3 --version
aws --version # awscli could be
sudo apt install python3-pip

pip3 install awscli --upgrade --user
aws --version
